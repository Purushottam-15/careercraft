{
  "name": "careercraft-root",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm install",
    "postinstall": "cd backend && npm install"
  }
}
